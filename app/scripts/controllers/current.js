'use strict';

/**
 * @ngdoc function
 * @name weatherlyApp.controller:CurrentCtrl
 * @description
 * # CurrentCtrl
 * Controller of the weatherlyApp
 */
angular.module('weatherlyApp')
  .controller('CurrentCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
